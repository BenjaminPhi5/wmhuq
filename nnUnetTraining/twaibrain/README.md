### training of nnUnet models

see `brainexperiments/run_nnUNet_v2/training` for the scripts used to train the nnUNet models.

auxilary code (model architecture, dataloading etc) is given in the `braintorch` folder.
